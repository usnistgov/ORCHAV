"""Bounded caches for reusable visualizer assets.

``cache_core`` defines byte-aware LRU policy. ``pygfx_mesh_buffers`` applies
that policy to prepared pygfx buffers and their optional disk sidecars.
"""
