"""Internal visualizer implementation packages.

The root ``visualizer`` package exposes the public app entry points. Modules
under ``visualizer.src`` contain the implementation layers for app composition,
services, renderers, scene conversion, panels, controllers, and diagnostics.
"""
