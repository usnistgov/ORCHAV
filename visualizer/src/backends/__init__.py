"""Convert shared scene data into backend-ready assets.

These helpers sit below the renderer protocol: ``open3d_scene_helpers`` loads
Open3D-native scene assets, while ``pygfx_scene_helpers`` prepares buffer and
texture payloads for the pygfx backend.
"""
