"""Capture renderer-neutral visualizer state for diagnostics.

Use ``debug_capture`` to collect screenshots, named-object inventories, and
compact ownership findings without depending on a backend's private objects.
"""
