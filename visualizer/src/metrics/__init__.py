"""Metrics helpers for canonical MPC data and statistics UI surfaces.

``packed_canon`` builds renderer arrays from compact frame projections,
``mpc_canon`` filters and colors those arrays, ``scenario_statistics`` streams
aggregates from selective projections, and ``viz_metrics`` owns the optional
live metrics window.
"""
