"""Pygfx notebook visualization and frame-regeneration workflows.

``pygfx`` provides the supported Qt-free scenario viewer for notebook cells
and standalone headless image export. ``generator`` wraps the offline Sionna
RT pipeline so notebooks can regenerate HDF5 frames and reload the same
visualizer object.
"""
