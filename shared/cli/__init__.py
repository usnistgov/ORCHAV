"""Shared command-line entry points.

``validate`` checks scenario YAML against the shared schema before a generator
or visualizer run. ``inspect`` reads generated frame or coverage HDF5 files and
prints the same logical information exposed through shared frame/coverage
contracts.
"""
