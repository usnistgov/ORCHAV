"""Vendor-neutral import example for externally produced ray paths.

Run :mod:`import_to_orchav` to adapt the source-owned records from
:mod:`fake_raytracer`, publish them as an ORCHAV frame set, and reload them
through the normal frame provider.
"""
